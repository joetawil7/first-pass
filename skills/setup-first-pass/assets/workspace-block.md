<!-- first-pass:workspace:start (written by the setup-first-pass skill from what it found; re-run it to refresh; your own notes go outside the markers) -->

## This workspace (first-pass)

{{summary}}

| Repo | What it is | Stack | UI | Group |
| --- | --- | --- | --- | --- |
{{rows}}

- **Which depends on which:** {{edges}}
- **Hooks first-pass runs from here** (a repo's own hooks do not fire when a session starts
  here): {{hooks}}
- **Each repo's commands, test limits and invariants** are in its own CLAUDE.md or AGENTS.md
  (the `first-pass:project` block) and its `INVARIANTS.md`.

<!-- first-pass:workspace:end -->
